package com.agriprocurement.common.domain;

/**
 * Marker interface for DDD Aggregate Roots.
 * An aggregate root is the main entity in an aggregate that ensures consistency
 * boundaries and controls access to other entities within the aggregate.
 */
public interface AggregateRoot {
}
